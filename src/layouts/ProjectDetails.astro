<!-- ====== PROJECT DETAIL ====== -->
        <section class="content-section hidden animate-fadeIn px-4" id="project-detail">
            <!-- Back Button -->
            <div class="mb-6 pt-4">
                <button id="projectDetailBack" class="inline-flex items-center gap-2 text-[1.15rem] text-gray-400 duration-300 group">
                    <svg class="transition-transform duration-300 group-hover:-translate-x-1" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
                    <span class="transition-transform duration-300 hover:scale-105" id="projectDetailBackText">Back to Projects</span>
                </button>
            </div>

            <!-- Hero Slider -->
            <div class="relative rounded-xl overflow-hidden border border-border mb-8 bg-dark-secondary aspect-auto" id="projectDetailSlider">
                <div class="slider-track" id="sliderTrack"></div>
                <button id="sliderPrev" class="slider-btn slider-btn-prev" aria-label="Previous image">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
                </button>
                <button id="sliderNext" class="slider-btn slider-btn-next" aria-label="Next image">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
                </button>
                <div id="sliderDots" class="slider-dots"></div>
            </div>

            <!-- Title & Description -->
            <div class="mb-8">
                <h1 id="projectDetailTitle" class="text-[1.75rem] max-sm:text-xl font-bold mb-3 text-gray-200"></h1>
                <p id="projectDetailDesc" class="text-lg max-sm:text-base text-gray-400 leading-relaxed mb-5"></p>
                <!-- <div id="projectDetailLinks" class="flex flex-wrap gap-3"></div> -->
            </div>

            <!-- Overview -->
            <div class="bg-dark-card border border-border rounded-xl p-6 max-sm:p-4 mb-5">
                <h2 class="flex items-center gap-2.5 text-lg font-semibold mb-4 text-gray-200">
                    <svg class="text-accent" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
                    Overview
                    <!-- <i class="fas fa-circle-question"></i>Overview -->
                </h2>
                <p id="projectDetailOverview" class="text-gray-400 leading-8 max-sm:leading-7 text-[1.15rem] max-sm:text-base text-justify"></p>
            </div>

            <!-- Features -->
            <div class="bg-dark-card border border-border rounded-xl p-6 max-sm:p-4 mb-5">
                <h2 class="flex items-center gap-2.5 text-lg font-semibold mb-4 text-gray-200">
                    <svg class="text-accent" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
                    Features
                </h2>
                <ul id="projectDetailFeatures" class="grid grid-cols-1 md:grid-cols-2 gap-3 text-[1.15rem] max-sm:text-base"></ul>
            </div>

            <!-- Tech Stack -->
            <div class="bg-dark-card border border-border rounded-xl p-6 max-sm:p-4 mb-5">
                <h2 class="flex items-center gap-2.5 text-lg font-semibold mb-4 text-gray-200">
                    <svg class="text-accent" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
                    Tech Stack
                </h2>
                <div id="projectDetailTech" class="flex flex-wrap gap-2"></div>
            </div>
        </section>